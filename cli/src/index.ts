/**
 * CLI entry point — a thin shell over `core` (CLAUDE.md: no business logic here).
 *
 * Placeholder for now: argument parsing and `--json` output land in a later
 * step. All scanning logic lives in `core`.
 */
export {};
